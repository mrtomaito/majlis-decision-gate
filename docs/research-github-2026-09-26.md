# ملحق — النظرة الخارجية ومفتاح الفرز

> تاريخ المسح: 2026-09-26 · يكمل `docs/research-github-2026-09-24.md` ولا يحل محله.
> **الحد:** قُرئ README وملف الرخصة للمستودعين أدناه، لا الشفرة. مستودع `calibrum` لا يُذكر في سجل المصادر: البحث أعاد عنه وصفين متعارضين ولم يُفتح README.

## الحكم

درجة ‎33 ليست احتمالاً. ما يمكن الاعتماد عليه اليوم هو احمرار البوابة، لا مفتاح الفرز. هذا الملحق لا يفتح قيداً ولا يستورد مستودعاً.

## ما قُرئ

| المصدر | ما ثُبت بقراءة الملف | ما لم يُنسخ ولماذا |
|---|---|---|
| [danilushin/asktheboard](https://github.com/danilushin/asktheboard) | README و`LICENSE`: رخصة MIT، حقوق 2026 Dan Ilushin. الصفحة العامة: 5 نجوم. التوقع لا يُسجَّل على موعد ماضٍ، ولا يُقيَّم قبل موعده، ووقت التثبيت والنص لا يتحركان عند التقييم | مقاعد الشخصيات تخرق «لا تقمّص». المبادئ الثلاثة صارت فقرة `confidence` في `docs/evidence-standard.md` |
| [MaxGhenis/brier](https://github.com/MaxGhenis/brier) → [ThesisInstitute/thesis](https://github.com/ThesisInstitute/thesis) | README يقول في آخره MIT. واجهة GitHub للرخصة على المستودع بعد التحويل: ملف `LICENSE` هو Apache-2.0، و4 نجوم في 2026-09-26. الإطار: فئة مرجعية قبل التعديل، وقاعدة حل، والتنبؤ رقم | التعارض بين جملة README وملف الرخصة يمنع النسخ. لا تُثبَّت الحزمة. المبدأ صار سطر `outside-view` |

فئة مرجعية (كانمان وتفرسكي، ثم فلايبيرغ): تُسمّى فئة وتوزيعها قبل الرقم الداخلي. لا توزيع سوقي بمصدر أولي في هذا المسح، لذلك الجملة المعتمدة: «لا توزيع — النظرة الداخلية ليست قراراً». التوزيع الوحيد المسموح درجة ج: ريال محصّل = 0 في هذه المحفظة حتى 2026-09-26، ومصدره `docs/decision-log.md`. ليس معدل نجاح الشركات.

## ما تغيّر في المستودع

- `docs/scoring-model.md`: المجموع مفتاح فرز، وعلامة `outside-view`.
- `docs/evidence-standard.md`: `confidence` هو التوقع، وتعديله بعد النتيجة ملء راجع. حد عرض Brier: 20 قيداً مقفلاً بـ`mover=stranger`، وتحتها المخرج «غير قابل للترتيب».
- `scripts/majlis-cli.ps1`: `triage` يطبع السطرين ولا يطبع `Ruling`.
- `scripts/audit-project.ps1`: فحص واحد يفشل إن غابت العلامة أو عُرض المجموع كحكم.

## ما لم يتغير

لا أوزان جديدة، ولا مهارة، ولا مجلس نماذج، ولا لوحة معايرة. `karpathy/llm-council` يبقى مؤجلاً كما في مسح 2026-09-24.
