# قائمة التنفيذ

- [x] جرد الملفات والروابط والمزاعم والتناقضات.
  - Verify: جرد 88 ملفاً؛ الروابط المحلية سليمة؛ فشل مدقق المهارات الجاهز بسبب `PyYAML` موثق.
- [x] كتابة مدقق المشروع وتشغيل الحالة الحمراء.
  - Verify: خط الأساس 14 فشلاً؛ وفشلت اختبارات المراجعة الجديدة قبل إصلاحها.
- [x] تصحيح نموذج الفرز والدرجات الحالية.
  - Verify: يعيد المدقق حساب المشاريع الثلاثة إلى 23 و20 و10 من 15 سجل دليل.
- [x] ترقية نقطة الدخول وحلقة الأسبوع.
  - Verify: الحالات الجافة الثلاث تنتج مشروعاً وقيداً واحدين وتمنع التواصل غير المأذون.
- [x] إضافة معيار الدليل وسجل المصادر.
  - Verify: كل مصدر خارجي له رابط ورخصة وقرار اعتماد أو رفض.
- [x] تأمين كتيبات التواصل والعروض والاقتصاديات.
  - Verify: موافقة مستقلة، ولا بيانات شخصية داخل المشروع، ولا ضمان أو إثبات اجتماعي مختلق.
- [x] تشغيل التدقيق ومراجعة النقض.
  - Verify: خروج المدقق بـ70 نجاحاً وصفر فشل، ومعالجة ملاحظات المراجعتين.
- [x] تحديث سجل القرار وقفل الجلسة.
  - Verify: قيد #004 مقفل للصنعة، والحالة الراهنة محدثة، والتقرير يبين الحدود والخطوة التالية.
- [x] ترقية المنظومة إلى 10/10 وتوسيع التدقيق الحتمي إلى 96 فحصاً.
  - Verify: بناء `scripts/majlis-cli.ps1` و`docs/entity-records-draft.md` و`.gitignore` واجتياز 96 فحصاً حتمياً من 96 بنسبة نجاح 100%.
- [x] تحديث وتحديث البنية المعمارية للأداة التشغيلية والتدقيق الحتمي (2026-08-23).
  - Verify: ترقية `scripts/majlis-cli.ps1` بدعم الفرز الديناميكي وأمر `doctor` التشخيصي ودعم الإخراج المهيكل `-Json`، وضمان التوافقية العابرة للأنظمة وترميز UTF-8، واجتياز 108 فحوصات حتمية من 108 بنجاح تام.
- [x] التطوير الشامل وسد الفجوات الميدانية وتوسيع التدقيق الحتمي (2026-08-27).
  - Verify: مزامنة مهارات `.claude` و `.agents` وتصحيح البادئات المبتورة، بناء كتيب الفحص التشخيصي 7-Point Audit ودليل شراكات مكاتب المحاسبة، ترقية `majlis-cli.ps1` بأوامر `pre-mortem` و `creative-angles` و `unit-economics` ودعم JSON، واجتياز 138 فحصاً حتمياً من 138 بنسبة 100%.


- [x] مراجعة كاملة وإصلاح سبعة عيوب مرصودة (2026-08-30).
  - Verify: تسريب مالي معلَّق (إيجار وحرق شهري) نُقل من `majlis-cli.ps1` إلى دفتر مستبعد من Git
    وفحصٌ يمنع عودته — **أُثبت بإعادة الرقم عمداً فحمره المدقق ثم أُزيل**؛ `AGENTS.md` و`.agents/`
    حُذفا وفحصٌ يفشل عند عودتهما؛ ثقب رؤوس القيود سُدّ فكشف ثلاثة قيود بلا عقد قياس فأُضيفت؛
    تثبيت مجاميع الفرز الثلاثة رُفع فصار المدقق يقارن المعلن بمجموع دليله — **أُثبت بتغيير 23 إلى 24
    فحمره المدقق**؛ تناقض 3/6 يوماً في `pre-mortem` أُزيل؛ وسيط `-Project` صار يرفض بدل أن يكذب.
  - Verify: **بوابة سوقية لها تاريخ انتهاء** — المدقق يقرأ `gate=` ويفشل إذا مرّ التاريخ والقائمة فارغة.
    **أُثبت بتقديم التاريخ إلى الماضي فخرج `EXIT=1`** ثم أُعيد. وسطر `MARKET` يُطبع آخر كل تشغيل مهما كان اللون.
  - Verify: ‏133/133 على نسخة المالك و133/133 على **نسخة مستنسخة** بُنيت من ملفات Git المرئية فقط
    واتُّبعت فيها خطوات README حرفياً؛ ومسح النسخة المستنسخة من الأرقام الخاصة والسجل التجاري خرج فارغاً؛
    و20 استدعاءً للأداة (10 أوامر × عادي وJson) خرجت كلها بصفر.
  - **ولا شيء مما سبق تقدم سوقي.** الخانات المملوءة **0 من 15** كما كانت في 2026-08-20، والموعد بعد أربعة أيام.

- [x] تثبيت فحوص حمراء ضد التقمص والتصديق القانوني الذاتي والوعود غير المسندة.
  - Verify: المدقق يفشل على أوامر `council` و`pitch-builder` والمواد التابعة لها قبل حذفها.
- [x] إزالة شجرة التعليمات الظلية والمواد المنسوبة خطأً والأوامر غير الحتمية.
  - Verify: غياب `AGENTS.md` و`.agents/` و`behavioral-alchemy`، واتساق الإحالات المحلية.
- [x] تشغيل الأوامر الحتمية المتبقية في الوضعين النصي وJSON.
  - Verify: كل أمر معلن يخرج بصفر وJSON صالح، والمدقق يعكس الفشل برمز خروج غير صفري.
- [x] التحقق من نسخة مستنسخة ثم تحديث السجل والإيداع.
  - Verify: ‏134/134 على نسخة مستنسخة بُنيت من فهرس Git وحده بسطور CRLF، بعد اتباع خطوات README
    حرفياً؛ ومسح المستنسخة من الملفات الخاصة خرج فارغاً؛ ومجموعة التعاقد خضراء فيها بـ`EXIT=0`.

- [x] إحياء مجموعة التعاقد الميتة وربطها بالموزّع (2026-09-03).
  - Verify: `tests/cli-contracts.ps1` كانت تستدعي `pwsh` غير المنصَّب فتموت كل حالة، وتخرج
    بـ`exit 1` عند النجاح، ويقتلها أول `stderr` متوقَّع. أُصلحت الثلاثة، وأُضيف حارس تكرار
    بمتغيّر بيئة يمنع قنبلة التفرّع بين `verify` والمجموعة.
  - Verify: أمرا `test` و`verify` في الموزّع بدعم `-Json` مُتحقَّق بالتحليل؛ وأربعة فحوص جديدة
    تقيس **قابلية التشغيل لا الوجود**؛ و‏134/134 مع `VERIFY : audit PASS | cli-contracts PASS`.
- [x] قراءة نتيجة قيد #001 في موعدها 2026-09-03.
  - Verify: الحكم **غير مُنفَّذ** (0 من 15) مقيَّد في `docs/decision-log.md` بقالب الإقفال
    الأسبوعي، مع نقض الفرضية وسبب الإبقاء على الأوزان. **لا أثر سوقي.**
