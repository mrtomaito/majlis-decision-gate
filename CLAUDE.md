# مجلس — مستشار الأعمال والتسويق للمحفظة

## ما هذا المشروع

المستشار الاستراتيجي والتنفيذي الذي نرجع إليه في كل قرار عمل، تسعير، تسويق، ومبيعات عبر المحفظة كلها.
**ليس مجرد مكتبة معرفة** — هو **بوابة قرار + محرك مبيعات صادر + حلقة محاسبة مالية**.

**المشكلة التي وُجد لأجلها:** واحد وعشرون مشروعاً حقيقياً تحت `C:\dev` وصفر ريال.
البناء والتنظيم يحدثان بامتياز؛ الشحن إلى السوق وتحصيل أول ريال هو الهدف الأوحد.

**معيار خروج المشروع نفسه:** أول ريال من أي مشروع قبل **2026-11-18** (تسعون يوماً من أول تشغيل).
إن مرّت 90 يوماً بلا ريال: **الخلل في البوابة لا في السوق** — تُراجع الأوزان، لا تُمدَّد المهلة.

---

## القوانين الخمسة الحاكمة (لا تُخرق)

1. **واحد فقط.** أي سؤال «وش أشتغل عليه» يُجاب بمشروع واحد باسمه + قائمة تجميد مسمّاة. الخروج بثلاثة ترشيحات = فشل البوابة.
2. **لا مشترٍ مسمّى، لا تواصل.** الشريحة واسم المنشأة لا يكفيان. يلزم `buyer-evidence-v1` كاملاً، بما فيه موافقة تسويق موثقة؛ وإلا فالمخرج بحث داخلي لا حملة.
3. **كل مرحلة سوقية تنتهي بشيء يراه غريب.** (موافقة، رفض، موعد، عرض استلمه، أو نقد دخل). البحث والتحضير شرطان تمهيديان يبقيان «مسودة» ولا يُسمّيان تنفيذاً سوقياً.
4. **لا تقمّص.** لا يُجاب بضمير «أنا» بصوت خبير. الصيغة دائماً: «إطار [المؤلف] يقول كذا، ودليله كذا، **ويفشل عندما** كذا».
5. **كل توصية تُقيَّد.** لا استشارة تنتهي بلا سطر في `docs/decision-log.md` فيه معيار نجاح برقم وتاريخ مستهدف.

<!-- market-stage-contract -->

---

## نقطة الدخول

كل سؤال يدخل من `skills/consultation`. وهي **تبدأ دائماً** بقراءة آخر خمس توصيات
في `docs/decision-log.md` ومعيار الدليل والحلقة الأسبوعية. الدين المتجاوز يُقفل قبل أي جواب جديد.

---

## الملفات المرجعية والكتيبات الميدانية

| الملف | ماذا يحمل |
|---|---|
| `portfolio.md` | الدفتر — صف لكل مشروع. **مصدر الحقيقة للبوابة** |
| `docs/scoring-model.md` | معادلة الفرز (v2.2) وبوابتا السوق والمشتري وسيناريو CVI |
| `docs/evidence-standard.md` | عقد المشتري ودرجات الدليل وفصل التنفيذ عن الصنعة والنتيجة |
| `docs/privacy-data-handling.md` | منع البيانات الشخصية داخل المشروع وعقد المخزن المقيد والموافقة |
| `docs/weekly-constraint-loop.md` | قيد واحد ومقياس واحد كل أسبوع |
| `docs/source-register.md` | المصادر التنظيمية والمفتوحة: ما تثبته، رخصتها، وقرار قبولها |
| `docs/gtm-line.md` | تسلسل التسويق السبعة وبواباته التنفيذية |
| `docs/playbook-weekly-sprint.md` | خطة السبرنت الأسبوعي للمبيعات (الأحد — الخميس) |
| `docs/playbook-b2b-outbound.md` | دليل المبيعات الصادرة ورسائل الواتساب والزيارات الميدانية للمنشآت |
| `docs/playbook-offer-engineering.md` | دليل اختبار العرض والسعر وحدود الاسترداد |
| `docs/playbook-objection-destruction.md` | مصفوفة تفكيك وتحطيم الاعتراضات التجارية في السوق السعودي |
| `docs/playbook-pre-mortem.md` | دليل فحص ما قبل الفشل (Pre-Mortem) والعكس الذهني |
| `docs/playbook-unit-economics.md` | دليل اقتصاديات الوحدة (CAC, LTV, Payback) وتدفق النقد |
| `docs/decision-log.md` | الحلقة — كل توصية ومصيرها ومعيار نجاحها |
| `docs/research-github-2026-08-20.md` | مسح المصادر المفتوحة والأطر الـ13 المستوردة |

---

## الحدود مع المشاريع الأخرى

- **`idea-appraisal-mihakk` (مِحَكّ):** يقيس **فكرة جديدة معزولة**. البوابة ترتّب **محفظة قائمة**.
  أي فكرة جديدة تظهر أثناء استشارة **تُحوَّل إلى مِحَكّ** ولا تُقيَّم هنا.
- **`~/.claude/brain/INDEX.md`:** يُقرأ لتغذية الدفتر وحقل «إعادة استخدام قدرة قائمة». لا يُنسخ محتواه هنا.
- **`C:\dev\PROJECTS-INDEX.md`:** المرجع لأسماء المجلدات ومستودعاتها. لا يُعاد إنتاجه.

---

## مصفوفة الأدوات والمهارات المتخصصة

| الحاجة والوظيفة الاستشارية | الأداة / المهارة | المصدر / الموقع |
|---|---|---|
| فرز المحفظة واختيار مشروع التركيز أو مرشح التحقق | `portfolio-triage` | `.claude/skills/portfolio-triage` |
| خطة الدخول الميداني للسوق | `gtm-line` | `.claude/skills/gtm-line` |
| اختبار العرض والسعر وحدود الاسترداد | `offer-builder` · `offers` · `pricing` | `.claude/skills/offer-builder` + عالمية |
| كتابة رسائل واتساب والمبيعات المباشرة | `outbound-engine` · `first-50-customers` | `.claude/skills/outbound-engine` |
| حساب اقتصاديات الوحدة وسرعة النقد | `unit-economics` | `.claude/skills/unit-economics` |
| كاسر الاعتراضات والتفاوض | `outbound-engine` · `docs/playbook-objection-destruction.md` | محلية + مساندة |
| الرسالة والنسخة الإعلانية النقية | `storybrand-messaging` · `copywriting` · `no-slop` | منصّبة ومستوردة |
| آراء متعددة لمجلس المسوقين | `marketing-council` | منصّبة (12 مسوّقاً) |
| أطر الكتب (هرموزي، روس، سيالديني، كرول، كيم، كريستنسن) | `vendor/wondelai/` (13 إطاراً) | مستورد MIT (راجع `LIMITS.md`) |
| القنوات (SEO، إعلانات، إيميل، إحالات) | `~/.claude/marketing-corpus/` | على القرص — تُقرأ لا تُنصَّب |
| نقض أي توصية تُنفق مالاً أو وقتاً | `doubt-driven-development` | منصّبة — إلزامية قبل الإعلان |

**قاعدة الدليل:** أي رقم خارجي قابل للتغير يحمل رابط مصدر أولي وتاريخ تحقق. وأي رقم داخلي
يحمل محدد سجل. التقدير يُوسم «تقدير» ولا يُعرض كقياس. المؤلف يقدّم الدليل ولا يصدر حكم نجاح عمله.

---

## الحالة الراهنة — 2026-09-06

- **المستودع منشور:** https://github.com/mrtomaito/majlis-decision-gate (عام · MIT).
  الإطار عام والدفتر خاص: الدفتر وسجل القرارات وسجلات الكيانات ودفتر اقتصاديات الوحدة
  **وملفات أي عميل** مستبعدة في `.gitignore` ولها نظائر `*.example.md` منشورة.
- **تنبيه عند كل قفل جلسة:** هذا الملف **منشور**. لا اسم شخص ولا رقم ولا حالة خاصة في هذا القسم —
  الإحالة إلى `docs/decision-log.md` غير المنشور، والتفصيل هناك.
- **قيد #001 حكمه «غير مُنفَّذ»: 0 من 15، والبوابة حمراء.** والجلسات المستهلكة في الصنعة **تسع**.
  **الفرضية منقوضة:** العائق ليس غياب قائمة الكيانات، بل أن وقت الجلسة يذهب إلى الصنعة كل مرة.
- **عقد القيد صار `constraint-contract-v2`** (المتن في `docs/evidence-standard.md`): يضاف إلى
  الحقول القديمة ثلاثة — `mover=self|stranger` (من يجب أن يتحرك ليتحرك الرقم)، و`status`،
  و`confidence` عدداً 0–100 على القيد المفتوح وحده.
- **قاعدة المقياس الذي لا تُحرّكه وحدك:** أي رقم يحرّكه المالك منفرداً **بديل**، وسنده الخارجي
  ورقة *Against Proxy Optimization* (`arXiv 2606.23597`). ويفرض المدقق أن يبقى قيد مفتوح واحد
  على الأقل بـ`mover=stranger`.
- **جهاز الإلزام يعمل الآن:** ما دامت البوابة حمراء لا يُسمح إلا بقيد صنعة **واحد** مفتوح.
  التحذير النثري لا يوقف أحداً؛ الفحص يوقف. **ولا ترفع الحالة إلا خانة مملوءة** — لا صنعة إضافية
  ولا تمديد موعد. **وتعديل `gate=` هرباً من الاحمرار ارتداد.**
- **المدقق 147/149 بفشلين مقصودين** (البوابة وجهاز الإلزام) عبر `majlis-cli.ps1 verify`.
- **فحصُ الوجود ليس فحصَ التشغيل، ولا فحصَ الاحمرار.** كل فحص جديد يجب أن يُرى محمرّاً على عيب
  مصنوع قبل اعتماده. ومجموعة الطفرات في `tests/cli-contracts.ps1` تفعل هذا آلياً على **نسخة
  مستنسخة**، وقد تحققت بالتخريب: لُيّن فحص فأمسكته.
- **`AGENTS.md` و`.agents/` حُذفا للمرة الثالثة.** والدرس أن المنع الوثائقي فشل ثلاث مرات:
  لا يملك ملف داخل المستودع منع مولِّد خارجي من الكتابة. **الحل في المنشأ** — إيقاف `Codex`
  على فرع `codex/full-audit-2026-09-01` — والمدقق يبقى صارماً بلا تليين.
- **قاعدة سجل قائمة:** لا يُقيَّد رقم مدقق ولا أمر جديد في سجل القرارات إلا بعد تشغيل يُظهره في
  موزّع أوامر `scripts/majlis-cli.ps1`، مع لصق سطر «‏Audit summary» الحرفي.
- **معلَّق:** دعوتا مساهمة لم تُرسَلا؛ وسجل المصادر مُتحقَّق قبل 17 يوماً (داخل نافذة التسعين).

**أول خطوة للجلسة القادمة:** خانة واحدة — `ER-KADR-001` باسم منشأة حقيقي ورابط عام تفتحه فعلاً،
و`verified-at` يُكتب بعد الفتح لا قبله. ثم `majlis-cli.ps1 verify-entity`.
**لا فحص جديد ولا كتيب جديد ولا تحليل محتوى خارجي ولا إصلاح أدوات قبل الخانة الأولى.**

## قواعد كتابة

- **الهرمية إلزامية:** الثوابت هنا فقط. **السقف 10,000 حرف لا 10 كيلوبايت** — القياس البايتي
  يعاقب العربية ٥٠٪ بلا سبب، والمدقق يقيس بالحروف. التفصيل المؤرَّخ في `docs/`.
  القرارات الجديدة تُضاف إلى `docs/decision-log.md`. **لا يُضخَّم هذا الملف.**
- **ممنوع إنشاء `AGENTS.md` و`.agents/`** — يحجبان هذا الملف عن Codex. **والمدقق يفشل عند عودتهما**
  (عادا في 2026-08-27 بمسارات `~/.Codex/` ميتة، وحُذفا ثانيةً في 2026-08-30).
- الوثائق بالعربية. أسماء الملفات والمجلدات لاتينية `kebab-case`.
